# SME Feedback Log — MVP Ventilator Simulator

Purpose: turn a trickle of SME feedback into visible patterns. Every piece of
reviewer feedback gets one row in the Intake ledger; recurring themes get tracked
separately so convergence becomes obvious. This log is for triage and
prioritization — it is not a commitment to build everything in it.

## How to use this log
1. When feedback arrives, add a row to the **Intake ledger** with the next
   SME-### id. Capture it even if details are incomplete — mark status `new` and
   note "awaiting detail."
2. Pick a **bucket** and a **severity**. If it doesn't sort cleanly, use the
   `Needs-investigation` bucket rather than forcing it.
3. When two or more reviewers raise the same thing, add or update a row in the
   **Theme tracker** and link the item ids. Patterns — not single opinions —
   drive what we fix.
4. Update **status** as items move: new → investigating → confirmed → planned →
   done (or wont-fix).
5. Fix order, per project policy: blockers and clear bugs first, then
   physiology/waveform concerns, then usability, then feature requests.

## Conventions

### Buckets
- **Bug** — broken or incorrect behavior the app shouldn't do.
- **Physiology** — waveform / mechanics / monitored-value concerns about clinical correctness.
- **Usability** — navigation, layout, labels, controls, discoverability.
- **Feature** — requests for new capability or future ideas.
- **Needs-investigation** — doesn't sort cleanly yet; could be bug vs. correct physiology, or detail still pending.

### Severity
- **blocker** — prevents teaching use or actively misleads learners.
- **should-fix** — meaningful problem; fix before a learner pilot.
- **nice-to-have** — polish or enhancement; safe to defer.

### Status
`new` · `investigating` · `confirmed` · `planned` · `done` · `wont-fix`

### Implicated layer (danger map)
Where the change would likely live, so we can gauge review risk early:
`lung-model.js` (high) · `ventilator.js` (high) · `simulation.js` (very high) ·
`waveforms.js` (medium) · `main.js` (med-high) · `alarms.js` · `style.css`
(low-med) · `test-engine.js` (high importance) · `docs` · `unknown`

## Intake ledger

| ID | Logged | Reviewer (role) | Bucket | Severity | Status | Summary | Implicated layer | Reproduction / detail |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SME-001 | 2026-05-30 | Christian (self, RT) | Needs-investigation | should-fix | confirmed-fixed | Missed patient triggers when patient rate set above ~22/min, in VC-CMV and PC-CMV | simulation.js / ventilator.js (trigger logic) | Set patient effort on, raise patient RR above ~22; some efforts fail to trigger. Adjudicate bug vs. realistic ineffective triggering against Mireles-Cabodevila PVI papers before any fix. CORROBORATED by SME-004 (Scott Mahoney) independently. Mechanism located: js/simulation.js:352 phase-gated trigger latch; efforts whose neural onset lands during machine INSPIRATION/HOLD are silently dropped. Read-only recon complete; sweep complete. RESOLVED — confirmed clinically by Christian 2026-08-05. Root defect fixed by the three-gate trigger-eligibility rule (PR #8, commit 146e6f8); made visible by the amber ineffective-effort highlight (0c5c79a), the per-gate tooltip (SME-022) and the Ineffective N /60s counter (cbe7d50). |
| SME-002 | 2026-05-30 | TBD | Usability | should-fix | done | Patient-effort sliders (Pmus/T-neural) run off-screen out of sidebar; units hidden unless hovered | style.css / main.js | Reported by Denis C and Scott Mahoney. Pmus/T-neural sliders extend past the sidebar edge; units only visible on hover. See also SME-010 (slider precision). DONE — split verdict. The horizontal spill was already fixed by the earlier effort-cluster sizing pass (commit 5e0260d); screenshot-verified at 240 px, zero overflow. The residual defect was the units: there is no `:hover` rule anywhere in style.css that hides them — the Effort row simply had NO inline value at all (its value lived only in the collapsed group header), and T-neural rendered a bare `1.0s`. Fix: inline `cmH₂O` value on the Effort row, spaced `1.0 s` on T-neural, unit+range `title` on both sliders, and `flex:0 0 auto` on the value so the range can no longer squeeze it into truncation. |
| SME-003 | 2026-05-30 | TBD | Needs-investigation | should-fix | done | Audible alarm not heard when alarms trip (may be user error / missing arm gesture) | alarm-audio.js / main.js | Reported by Rebecca Downs and Karen/Sarah; John C. Frostad noted a no-sound environment. Visual alarm fires but no audible tone. Diagnose user-error / missing arm-gesture vs. real defect before confirming. See SME-007. RESOLVED (real defect, not user error): volume raised + urgency-tiered tones added (PR #12, commit 4ea15c1/eea373d); time-base phantom silence fixed (commit 078975c, SME-008). See "Audible alarm not heard" theme. |
| SME-004 | 2026-06-11 | Scott Mahoney (RT, Dir. Clinical Ed, 20+ yr) | Bug | blocker | confirmed-fixed | Trouble capturing patient effort in PC-CMV and VC-CMV; trying to make patient overbreathe set rate produced severe dyssynchrony he did not expect | simulation.js (trigger logic) | Independent corroboration of SME-001. Same root cause, confirmed. RESOLVED — confirmed clinically by Christian 2026-08-05. Root defect fixed by the three-gate trigger-eligibility rule (PR #8, commit 146e6f8); made visible by the amber ineffective-effort highlight (0c5c79a), the per-gate tooltip (SME-022) and the Ineffective N /60s counter (cbe7d50). |
| SME-005 | 2026-06-11 | Andrea Ritz (RN, 11+ yr) | Bug | blocker | confirmed-fixed | PC-CSV with patient RR set to 35: apnea alarm fired AND monitor displayed RR as 6 instead of ~35 | simulation.js / main.js (measured RR in spontaneous mode) | RECLASSIFIED then RESOLVED. The sweep did not reproduce this via the phase gate — the single-digit-RR + apnea profile comes from trigger insensitivity vs effort (weak Pmus or stiff Ptrig), i.e. correct physiology that lacked a visible cue, not a measured-RR calculation bug. The cue shipped: Teaching-Mode Set/Delivered/Patient rates, the amber highlight, and the Ineffective N /60s counter. Confirmed clinically by Christian 2026-08-05. |
| SME-006 | 2026-06-11 | Karen LaRoche & Sarah Malyon (Clinical Specialists, RT, 30+/15+) | Bug | blocker | confirmed-fixed | Measured rate did not match patient rate in spontaneous mode | simulation.js / main.js (measured RR) | Second report of the measured-RR-in-spontaneous-mode defect (with SME-005). Screenshot in email. Same reclassification and same resolution as SME-005. Confirmed clinically by Christian 2026-08-05. |
| SME-007 | 2026-06-11 | Rebecca Downs (RN, Sim Educator, 16 yr) | Bug | blocker | done | Triggered high-pressure alarm visually but no audible alarm | alarm-audio.js / main.js | Corroborates SME-003. Reviewer flagged possible user error. Diagnose before confirming. RESOLVED (real defect, not user error): volume raised + urgency-tiered tones added (PR #12, commit 4ea15c1/eea373d); time-base phantom silence fixed (commit 078975c, SME-008). See "Audible alarm not heard" theme. |
| SME-008 | 2026-06-11 | Scott Mahoney (RT, Dir. Clinical Ed) | Bug | should-fix | confirmed-fixed | Switching modes occasionally auto-silenced alarms for ~400-1300 s; reproduced in Chrome and Firefox | main.js / alarms.js | Possible learner-mislead (could mask apnea during a demo). Wide random-seeming range suggests a units/variable error. Hard to reproduce reliably. RESOLVED: Root cause was alarm-audio timers on sim-time; sim.reset() (mode/flow switch) stranded them. Fixed by moving getAlarmNowSec() to wall-clock (performance.now()). Confirmed live by Christian (mode switch no longer strands the silence timer); not covered by npm test (browser audio). Commit 078975c. |
| SME-009 | 2026-06-11 | Denis C (CHSOS) | Bug | should-fix | confirmed-fixed | Inspiratory hold in PC-CSV will not release after activation; VC-CMV and PC-CMV release correctly | simulation.js / main.js (hold logic, PC-CSV path) | Mode-specific, cleanly reproducible. FIXED — investigation showed the engine was already correct: effectiveHoldTime returns 0 in spontaneous modes, so PC-CSV never enters a HOLD phase (verified by trace). The defect was UI-only: the hold button stayed live in PC-CSV and latched in 'Release' because it read the mode-suppressed holdActive. Fix: disable/guard the inspiratory-hold control in spontaneous modes so the UI honors the engine's existing suppression — button greyed, label reset to 'Activate', holdTime cleared on entering a spontaneous mode. Verdict was (c) conceptual: an inspiratory hold is a passive-mechanics measurement maneuver (plateau pressure), ill-defined during active spontaneous breathing — consistent with Mireles-Cabodevila 2021 / 2014 respiratory-mechanics literature and current-gen ventilator convention. Engine/physics untouched; UI-only fix (main.js + css). Confirmed live by Christian: hold works/releases in VC-CMV & PC-CMV, unavailable in PC-CSV. Commit c45ed05 (branch fix/hold-control-spontaneous). |
| SME-010 | 2026-06-11 | Denis C (CHSOS) + Scott Mahoney | Usability | should-fix | partly-fixed | Trigger pressure slider too short/coarse (jumps 0.5 to 5.0, little precision); especially small in PC-CSV | main.js / style.css | Two reporters. Step tightened 0.5 → 0.1 (commit 93362ae). Residual length/precision concern folds into the SME-023 input-layer refactor. |
| SME-011 | 2026-06-11 | Joel (RT educator) | Usability | nice-to-have | new | Set vs. measured parameters are intermixed; should be grouped like a real vent screen (set together, measured together) | main.js / style.css | Greg touched on this too. |
| SME-012 | 2026-06-11 | Scott Mahoney | Usability | nice-to-have | done | Loops only display when Teaching Mode is OFF; wants loops available regardless | main.js | DONE. Loops were never actually blocked in Teaching Mode — `bindLoopToggle` had no mode check — but `setTeachingMode(true)` force-hid them on entry and had no `else` to restore them on exit, so the user's choice was silently discarded and never came back. That reads as 'loops don't work in Teaching Mode'. Fix: removed the force-hide entirely; loop visibility is now purely the user's, preserved in both directions. Verified: on→teach→still on, off→teach→stays off, and re-enable while Teaching Mode is on. |
| SME-013 | 2026-06-11 | Greg Carter (Program Dir, 30+ yr) + Scott Mahoney | Usability | nice-to-have | done | Want ventilator mode shown alongside measured values in Teaching Mode (for a vent/patient system check view) | main.js | Two reporters. DONE — mode row added at the head of the monitored-value column, Teaching-Mode only (the header already carries the mode in standard mode). Reuses the header's existing strings (modeLabel + control-variable tag: set-point / flow-cycled / ramp) rather than inventing new copy, so the column reads top-down as a system check: what the vent is set to do, then what it produced. |
| SME-014 | 2026-06-11 | Scott Mahoney | Usability | nice-to-have | done | Peak-pressure number scaling is visually distracting (draws the eye) | main.js / style.css / simulation.js | ROOT CAUSE MEASURED, not guessed (scratch/measure-pip.cjs, 25 s sample): #param-pip changed **36.4×/10 s across 4 different rendered widths (15.6→62.4 px)** at the panel's largest type size (26 px, 28 px in Teaching Mode), while every neighbour was stable — VT 5.6, RR 0.4, VE 0, MAP 0 changes/10 s. Cause: `measuredPIP` is a running within-breath max reset at each breath start, so the readout tracked the inspiratory ramp digit by digit (9.7→10.4→11.1→…) and its box grew and shrank as the digit count changed. The rival hypothesis — that the pressure waveform's y-axis was rescaling — was tested and RULED OUT (1 distinct axis render over 24 s). Fix: engine now latches `lastBreathPIP` at breath start and exposes `breathSummary.pipLatched`; the monitor reads that, so PIP updates once per breath like a bedside ventilator. `pip` stays LIVE and untouched because the high-pressure alarm reads it and must fire the instant pressure rises, not a breath late — asserted by test. Post-fix: ≤6 changes / 20 s, single width. |
| SME-015 | 2026-06-11 | Greg, Scott, Karen & Sarah, Joel (4 senior RTs) | Feature | should-fix | new | Want I-time and/or flow as a settable variable instead of (or alongside) I:E ratio; I:E-only is uncommon on modern vents and limits synchrony/intrinsic-PEEP teaching | ventilator.js / main.js | DEFERRED to feature-review pass. Logged now because 4 independent senior-RT reports is itself the finding. |
| SME-016 | 2026-06-11 | Engine investigation (Vesper + parametric sweep) | Physiology | should-fix | done | Failed/ineffective patient efforts are dropped SILENTLY — no waveform marker, no counter — in both the phase-gate case (VC/PC-CMV overbreathing) and the trigger-insensitivity case (PC-CSV). Mireles-Cabodevila taxonomy treats ineffective triggering as a VISIBLE, diagnosable event. | simulation.js / waveforms.js / main.js | Unifying fix candidate for the trigger themes: render failed triggers as visible ineffective efforts (Pmus present, no Pvent). Addresses SME-001/004 (make drops honest+visible) and the SME-005/006 UX gap (show efforts failing the threshold). Design decision pending: (a) correct the phase-gate so drops occur only for physiological timing/threshold reasons, AND (b) surface failed triggers visually. Validate any fix across the full patientRR x I:E x hold space, not one operating point. COUNTER SHIPPED (the last missing piece of limb 2 / PR4): Teaching-Mode 'Ineffective N /60s' line under the Set/Delivered/Patient readout. Fixed 60 s window (matches triggerEventRetentionSeconds) rather than the display window, so the number keeps one meaning across the 5/10/20/30 s zoom levels. It counts BOTH failure modes, which matters: only `threshold` failures bend expiratory flow enough to draw the amber highlight, so the `ventilator_unavailable` phase-gate failures behind SME-001/004 have no waveform mark at all and this counter is the ONLY place they become visible. |
| SME-017 | 2026-06-16 | Christian (self, RT) | Bug | blocker | confirmed-fixed | Switching VC→PC spuriously fires an APNEA alarm on a non-apneic patient (backup rate 14, measured RR 14). Reproduced with the LOW-VE alarm on VC→PC switch too. The high-pressure alarm CLEARING on the switch is correct physiology (PC defaults PIP 20, below threshold) — the false apnea/low-VE firing is NOT. | alarms.js / simulation.js / main.js (alarm evaluation + sim.reset timing) | FIXED — root cause was a regression from the time-base PR (#13): getAlarmNowSec moved to wall-clock for the audio policy, but alarm EVALUATION differences nowSec against sim-time lastBreathStartSec, so wall−sim read as false apnea (and false low-VE via the stabilization grace not re-arming on reset). Fix: decoupled the two clocks in main.js — evaluation now uses sim.globalTime, audio keeps getAlarmNowSec() wall-clock. Confirmed live by Christian: VC→PC switch no longer fires false apnea, and SME-008 audio silence still holds across mode switch. Commit 921a469 (branch fix/alarm-eval-timebase). |
| SME-018 | 2026-06-16 | Christian (self, RT) | Usability | should-fix | done | No way to CANCEL an active alarm silence — once Silence is pressed, user must wait the full 120 s for it to expire; no un-silence/reset control. | main.js (silence control) | Add a toggle/reset so Silence can be cleared on demand. DONE — Silence is now a toggle: a second press clears `silencedUntilSec`, and because `lastSoundAtSec` is already stale the next frame sounds immediately, which is the point of cancelling. Found and fixed a second, worse half of the same bug while testing: `silenceBtn.disabled = !hasActiveAlarms` meant that if the alarm condition cleared mid-countdown the button greyed out **while still showing `Silenced 87s`** — the silence became genuinely uncancellable. Predicate is now `!hasActiveAlarms && !isSilenced`. Button title switches to advertise cancel. |
| SME-019 | 2026-06-16 | Christian (self, RT) | Usability | should-fix | new | Medium/low-urgency alarm is still too quiet and its repeat cadence too slow to draw attention. | main.js (medium volume) / alarm-audio.js (mediumRepeatSec) | Tuning: raise medium volume; shorten mediumRepeatSec (~30s→~12s). Couples with the deferred repeat-interval retune + TEST 52 update. High-priority alarm tuning is fine as-is per live testing. |
| SME-020 | 2026-06-16 | Christian (self, RT) | Bug | nice-to-have | new | Residual minor LOW-VE flicker: brief VE=0 blips at the stabilization-grace boundary and at VE-recompute gaps just after a mode switch (seen even on the correct sim-time base). Distinct from SME-017 (the false-apnea blocker, fixed) — this is a small pre-existing transient, deliberately scoped out of the SME-017 fix. | main.js (low-VE evaluation) / alarms.js (stabilization grace) | Polish: gate low-VE on "≥1 completed breath since reset" rather than a bare time threshold, so the grace can't expose a momentary VE=0. Low priority. |
| SME-021 | 2026-06-16 | Christian (self, RT) | Needs-investigation | should-fix | done | VC-CMV flow-trigger ineffective-effort: ~20 L/min trace swing, no trigger | simulation.js / waveforms.js | VERDICT: not a bug (correct behavior, literature-faithful). Read-only investigation via scratch/diag-trigger.mjs (per-tick real-engine snapshots). The trace plots signed net lung flow (currentFlow×60, simulation.js:744) — rendered accurately, no fidelity error. The trigger evaluates positive-only flow (max(0, currentFlow×60), simulation.js:402) against 2.0 L/min threshold. During this effort: pMus 2.0 is ~2 mL short of overcoming elastic recoil (V/C ≈ 2.03 cmH₂O), so net flow crests at −0.15 L/min and never crosses into positive territory — trigger-flow stays 0.000 for the whole effort. The ~20 L/min swing is mostly passive expiratory decay (−16 toward −0.15), bent upward by the effort; it is NOT inspiratory trigger signal. Two conflated quantities: TRACE shows total net flow (mostly expiratory), TRIGGER watches positive inspiratory-direction flow. Literature (Mireles-Cabodevila 2021): high-threshold failed trigger described as "flow crossing toward baseline but not reaching trigger threshold" — exactly this morphology. No code change. See follow-on: SME-022 (teaching annotation). |
| SME-022 | 2026-06-16 | Christian (self, RT) | Feature | nice-to-have | done (copy pending sign-off) | Teaching-mode annotation for ineffective-effort highlights | waveforms.js | When an ineffective effort is highlighted on the flow trace (existing amber highlight, _drawWaveformHighlights), add tooltip/label explaining WHY it failed — e.g. "effort bent expiratory flow toward baseline but did not generate positive inspiratory flow ≥ 2.0 L/min → failed trigger (Mireles-Cabodevila 2021)." Turns the counterintuitive 20-L/min-swing-no-trigger moment into a teachable lesson. Uses existing highlight primitive; Teaching-Mode only; display-only (no engine change). BUILT — display-only, no engine change, as scoped. The tooltip is now generated per failure mode from `gateFailed` and names the LIVE trigger setting rather than a hard-coded one (verified switching flow→pressure changes the copy from '2.0 L/min of inspiratory flow' to 'drop airway pressure by 3.5 cmH₂O below PEEP'). DEVIATION from the logged scope: the tooltip is available in both modes, not Teaching-Mode only — the amber highlight itself already draws in both, and a tooltip costs nothing until hovered, so gating it would leave standard mode showing an unexplained amber segment. The Teaching-Mode-only gate still applies to the on-canvas *label*. COPY AWAITING RT SIGN-OFF. |
| SME-023 | 2026-06-16 | Christian (self, RT) | Usability | should-fix | new (UI-refactor) | All settings use drag-based range sliders, which are awkward for FINE adjustment — hard to click-hold-drag to a precise value, especially on a laptop TOUCHPAD (easier with an external mouse). Sharpened by the trigger slider's new 0.1 step (finer granularity = smaller drag target per step). Affects the whole input layer, not one control. | index.html / css (all .control__range inputs) | Belongs to the UI-refactor horizon (do NOT patch one slider in isolation — would create inconsistency). Refactor should evaluate a non-drag input option: e.g. number field with +/− nudge buttons, or slider paired with a typable value, prioritizing touchpad usability for fine-step settings. Pairs with the set-vs-measured grouping item as a core control-layout decision. |

## Theme tracker (patterns across reviewers)

| Theme | Reports | Bucket | Severity | Status | Linked IDs | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Missed triggers at high patient rate | 2 | Needs-investigation | should-fix | done | SME-001, SME-004 | Confirmed by two independent reviewers. Mechanism located (simulation.js:352). Next: read-only parametric sweep, then fix-design decision. SWEEP COMPLETE (scratch harness vs real engine). Cliff is NOT fixed-rate — it slides with machine inspiratory dead-window: I:E 1:2 no-hold cliff ~RR 30; I:E 1:1 cliff ~RR 22; hold 0.5s cliff ~RR 24; PC-CMV ~RR 30. Reconciles SME-001's '~22' as an I:E 1:1 / hold condition (default settings would not show it until ~30). Mechanism = the old phase-gate (js/simulation.js:352 at the time); above cliff, dropped efforts were silent. CLOSED 2026-08-05: three-gate eligibility rule (146e6f8) plus the visibility layer (0c5c79a, cbe7d50). Confirmed clinically by Christian. |
| Measured RR wrong in spontaneous mode | 2 | Bug | should-fix (reclassified) | done | SME-005, SME-006 | Sweep did NOT reproduce SME-005 via the line-352 phase gate — PC-CSV tracks set rate under default effort. The single-digit-RR + apnea profile IS reproduced by TRIGGER INSENSITIVITY vs effort (weak Pmus or stiff Ptrig): e.g. Ptrig 5.0 at set-35 -> measRR 0 + apnea. Likely CORRECT physiology lacking a visible cue, not a measured-RR calculation bug. Separate root cause from SME-001/004. Reclassified from blocker-bug to correct-physiology-lacking-a-visible-cue. The cue shipped (Set/Delivered/Patient rates, amber highlight, Ineffective N /60s counter). CLOSED 2026-08-05, confirmed clinically by Christian. |
| Audible alarm not heard | 3 | Bug | blocker | done | SME-003, SME-007 (+ John C. Frostad noted no-sound environment) | RESOLVED. Volume raised (PR #12, commit 4ea15c1/eea373d) and urgency-tiered tones added (square triplet high / sine 2-note chime medium); time-base phantom silence fixed (PR fix/alarm-timebase, commit 078975c — resolves SME-008). Status: audible + tiered + reliable. Remaining: medium-alarm tuning (see new row). |
| Effort/trigger sliders off-screen or too coarse | 2 | Usability | should-fix | partly done | SME-002, SME-010 | SME-002 DONE (overflow was already fixed; the real residual was missing units — now inline with units on both effort sliders). SME-010 (slider precision/length) still open and folds into the SME-023 UI-refactor horizon. |
| I-time / flow control instead of I:E | 4 | Feature | should-fix | new | SME-015 | Four senior RTs; revisit in feature pass. |
| Alarm bugs surfaced in deep testing | 1 (self, deep testing) | Bug+Usability | mixed | mixed (17, 18 fixed; 19-20 open) | SME-017, SME-018, SME-019, SME-020 | Found while stress-testing the alarm subsystem. SME-017 false apnea/low-VE blocker FIXED (commit 921a469). SME-018 silence-cancel FIXED, and its uncancellable-after-alarm-clears variant fixed with it. Residual: SME-019 medium-alarm volume/cadence tuning and SME-020 minor low-VE flicker — both still open, both need an RT ear/eye rather than a code decision. |

## Reading the log (current priorities)

- **2026-08-05: the trigger cluster is closed.** SME-001, SME-004, SME-005 and
  SME-006 — three of them logged as blockers — are confirmed resolved
  *clinically* by Christian, which is the test that matters: the code changed in
  June, but an SME's experience is what closes an SME item. SME-001/004 were a
  real defect (the phase-gate silently dropped efforts) fixed by the three-gate
  eligibility rule. SME-005/006 were **not** a measured-RR calculation bug at
  all — they were correct physiology that the app gave the learner no way to
  see, and they closed when the visible cue shipped, not when a calculation
  changed. Worth remembering the next time a report looks like a wrong number.
- **The genuinely open set is now five, and all five need judgment rather than
  engineering:** SME-015 (I-time/flow settable — four independent senior-RT
  reports, still the strongest signal in the log), SME-023 + SME-011 (the
  input-layer and set-vs-measured refactor — one decision, not three),
  SME-019 (medium-alarm volume and cadence, needs an RT ear), SME-020 (low-VE
  flicker, low priority). SME-010 is partly fixed and folds into SME-023.
  SME-022 remains `done (copy pending sign-off)`.

- **2026-07-29 batch closed seven items** (SME-002, 012, 013, 014, 018, 022 plus
  the SME-016 ineffective-effort counter). Two defects in that batch were found
  by screenshotting the running app, not by reading code, and neither was in the
  original ticket: the Teaching-Mode RR readout was **clipped by the 208 px
  monitor column** (SET and PATIENT lost their digits — the dual-rate readout
  shipped in PR #11 has been unreadable in Teaching Mode ever since), and the
  Silence button **greyed out mid-countdown** when the alarm condition cleared,
  making the silence uncancellable. Keep screenshot-verifying UI work.

- **Flow-trigger investigation resolved (SME-021).** The apparent contradiction
  — a ~20 L/min swing on the flow-time trace with no trigger — was investigated
  via per-tick real-engine diagnostics (scratch/diag-trigger.mjs). Verdict: not a
  bug. The trace plots total net lung flow (signed, expiratory-negative); the
  trigger watches positive (inspiratory-direction) flow only. The swing stays
  below zero (mostly passive exhalation bent by effort); trigger-flow never
  crosses into positive territory. Literature confirms this as correct physiology
  (Mireles-Cabodevila 2021: high-threshold failed trigger, flow toward baseline
  but sub-threshold). Follow-on: teaching annotation to explain the morphology
  (SME-022 — since built; copy still awaiting RT sign-off).
- **Historical, for the record — how the trigger cluster was diagnosed.** The
  sweep (scratch harness vs real engine) showed the cliff was not fixed-rate: it
  slid with the machine inspiratory dead-window, ~RR 22 at I:E 1:1 up to ~RR 30
  at the default I:E 1:2, which reconciled SME-001's reported "~22" as an
  I:E 1:1 condition. That plus the silent-drop defect gave the two limbs of the
  fix, and both were unified by SME-016: render ineffective efforts visibly
  (Pmus present, no Pvent). All closed 2026-08-05.
- Audible alarm (SME-003/007) is RESOLVED: volume, urgency-tiering, and the
  time-base phantom-silence fix shipped (PRs #12, #13). What remains in the alarm
  area is SME-019 (medium-alarm volume and cadence) and SME-020 (minor low-VE
  flicker). SME-017 (false apnea on mode switch) and SME-018 (silence-cancel)
  are both fixed.

Last updated: 2026-08-05.
